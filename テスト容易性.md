# テスト容易性
## 1. はじめに
- ドメイン駆動設計の実装を学習している中で気づいたテストに関すること

個人的にドメイン駆動を勉強するために、KIROにサンプルとなるようなシステム（承認フローがあるMDMシステム）のドメイン駆動設計をしてもらって、一緒に実装を進めています。

その中で思ったことをご紹介

# 2. DDDの実装順と、初期フェーズで起きること
DDDでは実装順が次のようになります。
- Phase 1: ドメイン層
  - 値オブジェクト → 集約・エンティティ
- Phase 2: 承認ワークフロー（ドメイン層）
  - ビジネスルールと状態遷移の実装
- Phase 3: インフラストラクチャ層（リポジトリ）
  - 集約を永続化する仕組みを構築
- Phase 4: アプリケーション層（ユースケース）
  - 集約とワークフローを組み合わせて業務処理を実装
- Phase 5: プレゼンテーション層（REST API + UI）

初期スプリント（Phase 1-4）では画面が存在しない。
そのため、スプリントレビューで「動く画面」を見せることができません。

# 3. 初期スプリントでの“成果物”とは何か
UIがない代わりに、レビューで見せるべきものは ドメインの品質そのもの です。
具体的には：
- ユースケースの定義
- ドメインモデル（エンティティ・値オブジェクト・ルール）
- ドメイン層のテスト
- テストケースのカバレッジ
- テスト結果レポート
これらが UIの代わりに価値を示す成果物 になります。

# 4. テストの役割の変化
従来の単体テストは、「実装が期待通り動くか」を確認するもの

DDDの場合のテストの役割
- ドメインルールが正しく表現されているか
- ユースケースの振る舞いが意図通りか

テストは システムの価値を説明するための成果物 になるので
テストの作り方も変わってくる

従来：「メソッドごとにテスト」
DDD：「ビジネスルールごとにテスト」

例：
- ✅ `申請者本人は自分の申請を承認できない()`
- ✅ `承認期限は申請から1週間以内である()`
- ✅ `営業部内変更は1人の部長承認で完了する()`


# 5. テスト容易性を支えるツール
ドメイン層・インテグレーション層の品質を示すために、便利なツール
- Surefire（テスト結果のレポート化）
- JaCoCo（カバレッジレポート）

# 6. プロパティテストの紹介

## プロパティテストとは
プロパティテストは、**入力値の性質（プロパティ）を定義して、その性質を満たす大量のランダムな入力値でテストを自動実行**する手法です。

特徴：
- **自動で1000件実行**：jqwikはデフォルトで1000回のテストケースを自動生成・実行
- **シュリンク機能**：テストが失敗した場合、失敗する最小の入力値を自動で見つけてくれる
- **網羅性**：人間が思いつかないエッジケースも自動で発見

### jqwik: プロパティテストのフレームワーク 

## 固定値テスト vs プロパティテスト
従来のテストは固定の入力値でテストします：
```java
@Test
void 法人名は100文字以内である() {
    // 固定値でのテスト
    Customer customer = new Customer();
    customer.name = "A".repeat("株式会社テスト");
    assertTrue(customer.isValid());
}
```

プロパティテストは **入力値の性質を定義** してテストします：
```java
@Property
void 法人名は100文字以内である(@ForAll("validCustomerNames") String name) {
    // 性質：「100文字以内の任意の文字列」で常に成功する
    CustomerName customerName = CustomerName.of(name);
    assertThat(customerName.value().length()).isLessThanOrEqualTo(100);
}
```

## jqwikフレームワークでの入力値の性質定義

jqwikでは `@Provide` メソッドで入力値の性質を定義します：

```java
@Provide
Arbitrary<String> validCustomerNames() {
    return Arbitraries.strings()
            .withCharRange('あ', 'ん')  // ひらがな範囲
            .ofMinLength(1)            // 最小1文字
            .ofMaxLength(100);         // 最大100文字
}
```

# 7. まとめ
- DDD導入初期はUIが出ない
- テストが成果物になる
- テストの役割が変わってくる
- テスト結果をまとめるツール
- プロパティテスト と Jqwik
